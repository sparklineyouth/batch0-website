# Batch0 MCP server

Local stdio MCP integration and matching CLI for Batch0 course operations.

```sh
npm ci
npm test
node dist/cli.js batch0_status
```

Read-only by default. See [setup, tool usage, publishing controls, and limitations](../docs/mcp.md). No credentials are stored in this package.
