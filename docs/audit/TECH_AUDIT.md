# TECH_AUDIT.md — stack, infra, and technical findings (BEFORE state)

## Stack
- Next.js 14.2.15 (App Router), React 18, TypeScript, Tailwind 3.4
- Supabase (data + auth), Stripe, Resend, Sentry; hosted on Vercel (batch0.org)
- Dev server: npm run dev

## Live-domain checks (2026-07-10)
- robots.txt allows *; GPTBot, ClaudeBot, PerplexityBot all fetch / with HTTP 200 - the brief's "blocked crawler" finding does NOT reproduce on .org
- batch0youth.com does not resolve (HTTP 000) - likely source of the external report; logged in NEEDED_FACTS
- apex .org 307-redirects to www; metadataBase/sitemap/robots reference apex (inconsistency - see findings)

## Inventory

### Components
- Navbar (components/navbar.tsx) — client component; fixed top nav with logo, anchor links, Apply/Dashboard CTA, and a portal-rendered mobile drawer with focus trap. Used on / (app/page.tsx); LINKS use absolute /#anchor hrefs so it is designed for reuse on subroutes like /cohort/[slug] and /sponsors.
- Hero (components/hero.tsx) — RSC; headline, live-signal eyebrow pill (countdown/spots), dual CTAs, and a 4-up ProofStat <dl> row. Used on / only. Contains private ProofStat subcomponent.
- Marquee (components/marquee.tsx) — RSC, aria-hidden; infinite horizontal curriculum-keyword strip with edge fade gradients, driven by the animate-marquee keyframe. Used on / only.
- Problem (components/problem.tsx) — RSC; 'Youth entrepreneurship is broken' section: 5/7 grid with numbered <ol> of 3 problem items, scroll-revealed. Used on / only.
- Builds (components/builds.tsx) — RSC; 'What you'll learn' 6-card grid, each with a lucide icon chip (Compass, Wrench, LineChart, Megaphone, Presentation, Handshake). Used on / only.
- ScrollPreview (components/scroll-preview.tsx) — client; #how-it-works product mock. Mobile: 3 stacked feature cards; desktop: fake dashboard UI (sidebar + cards) inside ContainerScroll 3D zoom. Dynamically imported on / with an h-[60rem]/h-[80rem] placeholder. Pulls 17 lucide icons.
- ContainerScroll (components/container-scroll.tsx) — client; framer-motion scroll-linked 3D card (rotateX 20→0, scale 1.05→1, header translateY 0→-100) with matchMedia mobile + prefers-reduced-motion handling. Used only by ScrollPreview (desktop branch) on /. Aceternity-style component.
- Curriculum (components/curriculum.tsx) — RSC; #curriculum 4-week vertical timeline (Validate/Build/Market/Pitch) with phosphor dot markers on a 1px rail. Used on / only.
- Stats (components/stats.tsx) — RSC; tuition price splash (giant priceLabel) + 3 supporting stat cells with vertical-rule dividers. Used on / only.
- Sponsor (components/sponsor.tsx) — RSC; #sponsor 'Powered by Impetus AI' section: pulsing pill, 3 pillar cards, 3 stats, external CTA to impetusai.net + mailto. Used on / only.
- Comparison (components/comparison.tsx) — RSC; #compare competitor table (LaunchX/LeanGap/YEA!/batch0) as a desktop <table> + mobile stacked cards, with Check/Minus lucide ValueCell. Used on / only.
- FAQ (components/faq.tsx) — client; #faq single-open accordion (9 questions) with rotating Plus icon and grid-rows CSS height animation. Used on / only.
- CTA (components/cta.tsx) — RSC; #apply closing 2-column offer card (price + bullets | urgency pill + apply button), plus contact mailto. Contains private Bullet subcomponent. Used on / only.
- Footer (components/footer.tsx) — async RSC; 4-column footer (brand blurb, Program links, Connect links, legal row). Self-resolves SiteConfig if not passed. Used on / and importable by other layouts/pages.
- StickyMobileCta (components/sticky-mobile-cta.tsx) — client; mobile-only bottom-pinned apply bar that slides in after scrollY > 480; hidden when authed or applications closed. Used on / only.
- Reveal (components/ui/reveal.tsx) — client; IntersectionObserver scroll-reveal wrapper adding .revealed to the CSS .reveal class (framer-motion whileInView replacement). Used by problem, builds, curriculum, sponsor, comparison, cta on /.
- Button (components/ui/button.tsx) — RSC-safe button with primary/secondary/ghost/danger variants and sm/md/lg sizes, active:scale-[0.98] press. NOT used on the marketing homepage — used across /apply, /admin/*, /dashboard/*, etc. (20+ files).
- Card + CardHeader + StatusBadge (components/ui/card.tsx) — rounded-2xl zinc-900/40 surface, header row, and a status→color badge map. Not on marketing homepage; used across /apply, /verify, /admin/*, /dashboard/* (20+ files).
- ConfirmDialog (components/ui/dialog.tsx) — portal modal with backdrop blur, Escape/focus handling, destructive variant. Admin/dashboard only (refunds, deletes, bulk actions).
- Input / Textarea / Select / Label / FieldError (components/ui/input.tsx) — form controls with error-state classes (red border+ring+tint) and aria-invalid/describedby wiring. Used on /apply and throughout admin/dashboard/investor forms.
- LocalTime (components/ui/local-time.tsx) — client; hydration-safe local-timezone timestamp renderer (UTC first paint, local after mount). Admin/dashboard surfaces only.
- Toggle (components/ui/toggle.tsx) — client; role=switch card row with sliding knob. Admin settings/announcements/teams/events, dashboard check-in, investor SAFE form — not marketing.

### Fonts
NO web fonts are loaded anywhere — no next/font, no @font-face, no Google Fonts <link>, no font files in public/. The site runs entirely on the system stack. tailwind.config.ts extends fontFamily.sans to ["ui-sans-serif", "system-ui", "-apple-system", "Segoe UI", "Roboto", "Inter", "sans-serif"] — note "Inter" is listed in the stack but never actually loaded, so it only renders for users who happen to have Inter installed locally (a latent inconsistency). globals.css adds font-feature-settings: "ss01", "cv11" on html/body (Inter-specific feature flags that are no-ops on the actual system fonts). app/opengraph-image.tsx uses its own inline stack: "system-ui, -apple-system, Segoe UI, Roboto, sans-serif".

### Colors
DESIGN TOKENS (tailwind.config.ts): phosphor scale — DEFAULT #FFBB00, 50 #FEF9C3, 100 #FEF08A, 200 #FDE047, 300 #FFBB00 (dupe of DEFAULT), 400 #EAB308, 500 #CA8A04; ink scale — 950 #000000, 900 #0A0A0A, 800 #111111, 700 #1A1A1A, 600 #222222 (the ink scale appears UNUSED in components — surfaces actually use black/zinc utilities); backgroundImage tokens phosphor-grid + phosphor-radial use rgba(250,204,21,0.08/0.15). CSS VARS (globals.css): --background #000000, --foreground #fafafa. GLOBALS.CSS LITERALS: ::selection #ffbb00/#000; .legal-prose #ffbb00, #fde047, #ffffff, rgba(255,255,255,.85/.1); .glow-text/.grid-bg/.gradient-border/.shine all hardcode rgba(250,204,21,…) and #ffbb00/#fde047; .scrollbar-thin rgba(255,255,255,.15–.3); theme-light block hardcodes a whole parallel palette (#ffffff, #f4f4f5, #18181b, phosphor-on-light #b88e00, emerald #047857, red #b91c1c, amber #92400e, blue #1d4ed8). DE-FACTO PALETTE VIA UTILITIES (marketing components): bg-black, bg-zinc-950, bg-zinc-900, white at opacities /5 /10 /15 /25 /30 /40 /45 /55 /60 /65 /70 /75 /80 /85 /90 and arbitrary bg-white/[0.02–0.04], phosphor at /10 /15 /20 /30 /40 /60 /70 /90 plus arbitrary phosphor/[0.03–0.08], text-black on phosphor buttons. ROGUE HARDCODED ONE-OFFS in marketing components: (1) shadow-[0_8px_24px_-8px_rgba(250,204,21,0.5)] on primary CTAs in hero.tsx:71, sponsor.tsx:106, cta.tsx:90 — and a mismatched 0.6-alpha variant in sticky-mobile-cta.tsx:49; (2) navbar.tsx:94 drawer shadow-[0_0_60px_rgba(0,0,0,0.6)]; (3) container-scroll.tsx:97 bg-[#222222] (should be ink-600 token) and :94 inline six-layer boxShadow of raw 8-digit hex blacks (#0000004d, #0000004a, #00000042, #00000026, #0000000a, #00000003); (4) ui/dialog.tsx:72 shadow-[0_30px_80px_-20px_rgba(0,0,0,0.6)]; (5) app/opengraph-image.tsx inlines #000000, #FFBB00, #ffffff, rgba(250,204,21,0.22/0.35/0.1/0.55), rgba(255,255,255,0.6/0.45); (6) public/logo.svg gradient #FDE047→#EAB308 with #CA8A04 stroke; (7) app/layout.tsx themeColor #000000/#ffffff. Status colors (ui/card.tsx StatusBadge, app-side): blue-300/400, emerald-300/400, red-300/400/500, amber-300/400 at /15 opacities.

### Third-party scripts
Effectively none on the marketing page — no analytics, no tag manager, no pixel, no next/script or <Script> usage, no external <script src>. The only injected script is a first-party inline <script type="application/ld+json"> in app/layout.tsx with EducationalOrganization JSON-LD (fixed literal, dangerouslySetInnerHTML). The only third-party JS is @sentry/nextjs (v10.52) bundled via withSentryConfig in next.config.js — gated on NEXT_PUBLIC_SENTRY_DSN/SENTRY_DSN env, production-only, tracesSampleRate 0.1, replay off except on-error, tunneled through /monitoring (so no external script tag; it ships inside the bundle). Client-side deps that land in the marketing bundle: framer-motion v11 (only via ContainerScroll, dynamically imported below the fold) and lucide-react icons.

### Animation inventory
TAILWIND KEYFRAMES (tailwind.config.ts): animate-phosphor-pulse (phosphorPulse 2.4s, opacity + yellow drop-shadow glow) — used on the Sponsor section pill dot (sponsor.tsx:39) and app/(auth)/layout.tsx; animate-marquee (marquee 40s linear infinite, translateX 0→-50%) — used only in marquee.tsx:35; animate-fade-up, animate-fade-in, animate-slide-down — DEFINED BUT UNUSED (fade-up only mentioned in a reveal.tsx comment); built-in animate-ping — hero eyebrow live dot (hero.tsx:48); built-in animate-spin — app-side loading spinners only (apply form, admin, dashboard), not marketing. SCROLL-REVEAL SYSTEM: .reveal/.revealed CSS classes in globals.css (opacity 0 + translateY(20px) → visible, 600ms cubic-bezier(0.16,1,0.3,1)) driven by the IntersectionObserver <Reveal> component; used in problem, builds, curriculum, sponsor, comparison, cta with staggered inline transitionDelay (60–160ms steps). FRAMER-MOTION: only components/container-scroll.tsx — useScroll + useTransform mapping scrollYProgress to rotateX 20→0 (10→0 mobile), scale 1.05→1, header translateY 0→-100; desktop-only branch of ScrollPreview, dynamically imported; honors prefers-reduced-motion by zeroing ranges. TRANSITION CLASSES / TACTILE SYSTEM (globals.css): global button/a/[role=button] transition-duration pinned to 0ms for instant state flips; .press class = transition:none + :active scale(0.98); Button component uses active:scale-[0.98]. Opt-back-in transitions: FAQ accordion grid-template-rows+opacity 300ms ease-out + Plus icon rotate-45 200ms (faq.tsx); sticky mobile CTA translate/opacity 300ms ease-out slide-up (sticky-mobile-cta.tsx:40); CTA/hero/sponsor arrow transition-transform group-hover:translate-x-0.5; sponsor pillar cards transition-colors on hover; Toggle knob transition (app-side). ACCESSIBILITY: global prefers-reduced-motion media query in globals.css nukes all animation/transition durations to 0.001ms; html scroll-behavior:smooth (also reset under reduced motion). UNUSED DECORATIVE CSS: .glow-text, .grid-bg, .gradient-border are defined in globals.css but referenced nowhere; .shine (animated-looking gradient text-clip, static) used in hero.tsx, scroll-preview.tsx, app/sponsors/page.tsx; bg-phosphor-radial used on app/apply + auth layout only; bg-phosphor-grid unused.

### Image inventory
public/ contains exactly ONE asset: public/logo.svg — a 200x200 lightning bolt, linearGradient #FDE047→#EAB308, #CA8A04 stroke, feGaussianBlur glow filter. Referenced via next/image in navbar.tsx (26px header + 24px mobile drawer), footer.tsx (28px), scroll-preview.tsx (20px inside the fake dashboard sidebar), and as favicon/shortcut/apple icon in app/layout.tsx metadata + JSON-LD logo URL. NO photos, no illustrations, no team/testimonial imagery anywhere on the marketing surface — the entire visual layer is CSS + lucide-react inline SVG icons. Lucide icons on marketing: navbar (Menu, X), builds (Compass, Wrench, LineChart, Megaphone, Presentation, Handshake), comparison (Check, Minus), faq (Plus), scroll-preview (Home, FileText, PlayCircle, ClipboardList, CheckCircle, CalendarDays, FolderArchive, MessagesSquare, Sparkles, FolderOpen, CreditCard, Settings, Bell, ArrowRight, Users, Trophy — Users imported but unused). Other inline SVG: the .noise utility in globals.css embeds a data-URI feTurbulence SVG (defined; used on app-side pages, not marketing homepage). Dynamic image: app/opengraph-image.tsx generates the 1200x630 OG card at request time via next/og ImageResponse (black bg, yellow radial glow, square logo stand-in — it does NOT use the actual bolt logo). All decorative dots/rails/dividers (hero ping dot, curriculum timeline dots, marquee fades, sponsor top rule) are pure CSS elements, not images.

## Technical findings
See "Technical/marketing tells" in SLOP_REPORT.md (merged, severity-sorted).

## Tech auditor notes

Audit of /Users/rishabhdagli/Documents/batch0-website (Next.js 14.2.15, deployed at batch0.org). Apply-funnel trace (task 8): logged-out click on any Apply CTA → <a href="/apply"> → middleware (lib/supabase/middleware.ts:105-112) 307s /apply → /signup?next=%2Fapply (mobile sticky CTA users land on /signup) → signup-form.tsx runs signUpAction (server-side admin.createUser with email_confirm:true, no email verification) then signInWithPassword → router pushes next → /apply (app/apply/page.tsx requireUser) → ApplicationForm autosaves drafts and submits via app/apply/actions.ts to the `applications` table (strict zod SubmitSchema: why_join ≥40 chars, parent_email required under 18, cohort validated against open cohorts; drafts rate-limited 30/min) → redirect /dashboard/application?submitted=1, emails via Resend + admin notifications + optional Discord embed. The form pipeline is genuinely solid. No analytics of any kind exist (task 8 confirmed: only Sentry). OG image (task 2): on-brand dark/#FFBB00 card with live cohort+price pill — good, not slop, but carries the false '4-week' claim. Biggest risks ranked: (1) 4-week claim vs 6.5-week real cohort everywhere including <title> and OG image, (2) mentor/investor-network/community claims with 0 mentors/0 investors/0 enrollments in a paid program for minors, (3) uncrawlable+unmeasured conversion funnel, (4) apex-vs-www split with no canonicals, (5) Terms naming 'batch0' instead of Impetus AI LLC. No href=\"#\" or unsafe target=_blank found in marketing components (sponsor.tsx external link has rel=\"noopener noreferrer\").
